export enum tagTypes {
  user = "user",
  profile = "profile",
  users = "users",
  category = "category",
  service = "service",
  order = "order",
  portfolio = "portfolio",
  // pre
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.profile,
  tagTypes.users,
  tagTypes.category,
  tagTypes.service,
  tagTypes.order,
  tagTypes.portfolio,
  //pre
];
